version: 2

build:
  os: "ubuntu-20.04"
  tools:
    python: "3.8"

sphinx:
  configuration: docs/conf.py
  fail_on_warning: true

formats: all

python:
  version: "3.8"
  install:
    - method: pip
      path: .
    - requirements: docs/requirements_docs.txt

submodules:
  include: all